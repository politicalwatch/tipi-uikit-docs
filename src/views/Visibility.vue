<template>
  <div class="container">
    <h2>Visibility</h2>
    <p>Clases específicas para controlar la visibilidad de un elemento</p>
    <ul>
      <li><code>.u-block[@(breakpoint)]</code>: establece el valor <code>block</code> a la propiedad <code>display</code></li>
      <li><code>.u-inline-block[@(breakpoint)]</code>: establece el valor <code>inline-block</code> a la propiedad <code>display</code></li>
      <li><code>.u-inline[@(breakpoint)]</code>: establece el valor <code>inline</code> a la propiedad <code>display</code></li>
      <li><code>.u-hide[@(breakpoint)]</code>: establece el valor <code>none</code> a la propiedad <code>display</code> en el breakpoint específico</li>
      <li><code>.u-hide-overflow</code>: establece el valor <code>hidden</code> a la propiedad <code>overflow</code></li>
    </ul>
  </div>
</template>
